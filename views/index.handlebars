<h1>Welcome to the burger bar!!!</h1>
{{!-- 
<div>
    <img src="/../public/assets/img/burger.png">
</div>
 --}}

<h2>Eat that burger</h2>


<ul>
    {{#each burgers}}
        {{#unless devoured}} 
            {{> burgers/burger-block devoured=false}} 
        {{/unless}} 
    {{/each}}
</ul>

<h2>Devoured burgers</h2>

<ul>

    {{#each burgers}}
        {{#if devoured}} 
            {{> burgers/burger-block devoured=true}} 
        {{/if}} 
    {{/each}}
</ul>

<h2>Add a burger to the devour queue</h2>

<form class="create-form">

    <div class="form-group">
        <label for="bu">Add a Burger:</label>
        <input type="text" id="bu" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">Thats a good burger!</label>
        <br>
        <input type="radio" name="devoured" value="0" checked>Hungry for a burger!?!  Add that craving to your devour queue.
        <br>
        <input type="radio" name="devoured" value="1">Like the taste of that burger?  Add to your list of devoured burgers.
    </div>

    <button type="submit">Add your burger</button>
</form>